import type { WritingEntry } from "@/types/content";

/**
 * Add new writing here. Each string in `body` renders as its own paragraph.
 * Newest pieces should go first.
 *
 * Example:
 * {
 *   slug: "a-short-url-friendly-name",
 *   title: "The title of the piece",
 *   date: "September 3, 2026",
 *   excerpt: "An optional one-line preview.",
 *   body: ["First paragraph.", "Second paragraph."],
 * }
 */
export const writingEntries: WritingEntry[] = [];
